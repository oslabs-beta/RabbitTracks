// we grab messages and display.... can organize based on rows.... props it up to container

//use axios to grab messages from backend
import * as React from "react";

export const DeadLetterMessage = () => {
    //trying to see if I should fetch data here or in the container
    
}